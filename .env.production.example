# =============================================================================
# ContentFlow — Production Environment Variables
# =============================================================================
# Copy to .env and fill in values before deploying.
# NEVER commit .env with real secrets to version control.
# =============================================================================

# --- App ---
APP_ENV=production
APP_NAME=ContentFlow API
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO
STRUCTURED_LOGGING_ENABLED=true

# --- Supabase (Required) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# SUPABASE_DB_URL=postgresql://postgres:...@db.xxx.supabase.co:5432/postgres

# --- Redis (Required) ---
REDIS_URL=redis://default:password@host:6379/0
# CELERY_BROKER_URL=          # Falls back to REDIS_URL
# CELERY_RESULT_BACKEND=      # Falls back to CELERY_BROKER_URL

# --- Security (Required) ---
# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=
# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
OAUTH_STATE_SECRET=
OAUTH_REDIRECT_BASE_URL=https://contentflow-api.railway.app

# --- API Keys ---
API_KEY_PREFIX=cf_live
API_KEY_BYTES=24

# --- Monitoring ---
# SENTRY_DSN=https://xxx@sentry.io/yyy
# SENTRY_TRACES_SAMPLE_RATE=0.1
PROMETHEUS_ENABLED=true

# --- yt-factory (Video Generation) ---
# YT_FACTORY_BASE_URL=https://yt-factory.example.com
# YT_FACTORY_API_KEY=
# YT_FACTORY_TIMEOUT_SECONDS=900
# YT_FACTORY_POLL_INTERVAL_SECONDS=5.0

# --- OAuth: Google (YouTube + Google Business) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# --- OAuth: Meta (Instagram + Facebook + Threads) ---
# META_CLIENT_ID=
# META_CLIENT_SECRET=

# --- OAuth: TikTok ---
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=

# --- OAuth: X (Twitter) ---
# X_CLIENT_ID=
# X_CLIENT_SECRET=

# --- AI (Comment Autopilot + Viral Prediction) ---
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest
# ANTHROPIC_API_BASE_URL=https://api.anthropic.com/v1
# COMMENT_POLL_INTERVAL_SECONDS=300

# --- Trending (YouTube Data API) ---
# YOUTUBE_API_KEY=

# --- Railway ---
RAILWAY_ENVIRONMENT=production
